import PlaceholderPage from '../components/PlaceholderPage';

const LandingPage = () => (
  <div className="flex min-h-screen flex-col items-center justify-center bg-[var(--bg-base)] p-8">
    <PlaceholderPage
      title="TransitOps"
      phase="Phase 5"
      description="Public marketing site — landing page, features, and CTAs."
    />
  </div>
);

export default LandingPage;
